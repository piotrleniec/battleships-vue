machine:
  pre:
    - mkdir ~/.cache/yarn
  node:
    version: stable

dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn unit
